<h1>Welcome to PandaPlan!</h1>
<h3>PandaPlan is a lesson-plan organizer to prepare educators for technology lessons using the medical teaching paradigm 'See one, do one, teach one'.</h3>

<h1>List of my courses</h1>

<table class="table">
  <thead>
    <tr>
      <th>Course</th>
      <th colspan="3"></th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= link_to course.name, course %></td>
        <th colspan="3"></th>
        <td><%= course.description %></td>
        <th colspan="3"></th>
        <td><%= link_to 'Edit', edit_course_path(course) %></td>
        <td><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<input class="btn btn-primary" type="button" value="Make this a link to New Course">
<%= link_to 'New Course', new_course_path %>

